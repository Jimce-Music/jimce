{
  "name": "jimce",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest",
    "pino-pretty": "^13.1.3",
    "prettier": "3.7.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "scripts": {
    "run": "bun run src/index.ts",
    "dev": "docker compose -f compose-dev-db.yml up -d db && bun run src/index.ts | bun x pino-pretty && docker compose -f compose-dev-db.yml down",
    "prettier": "bunx prettier src/ --write"
  },
  "dependencies": {
    "fastify": "^5.6.2",
    "pino": "^10.1.0",
    "zod": "^4.2.1"
  }
}

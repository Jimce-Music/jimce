Just a short notice, this folder holds all boilerplate to automatically generate and publish the API clients via GitHub actions.

Current clients in this folder:

- TypeScript package in `typescript-api-client`
- Flutter package in `jimce_api_flutter`
